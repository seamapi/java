/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.seam.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.seam.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;

@JsonInclude(JsonInclude.Include.NON_EMPTY)
@JsonDeserialize(builder = DevicePropertiesDormakabaOracodeMetadataPredefinedTimeSlotsItem.Builder.class)
public final class DevicePropertiesDormakabaOracodeMetadataPredefinedTimeSlotsItem {
    private final String name;

    private final double prefix;

    private final String checkInTime;

    private final String checkOutTime;

    private final boolean is24Hour;

    private final boolean isBiweeklyMode;

    private final boolean isOneShot;

    private final boolean isMaster;

    private final double extDormakabaOracodeUserLevelPrefix;

    private final String dormakabaOracodeUserLevelId;

    private final Map<String, Object> additionalProperties;

    private DevicePropertiesDormakabaOracodeMetadataPredefinedTimeSlotsItem(
            String name,
            double prefix,
            String checkInTime,
            String checkOutTime,
            boolean is24Hour,
            boolean isBiweeklyMode,
            boolean isOneShot,
            boolean isMaster,
            double extDormakabaOracodeUserLevelPrefix,
            String dormakabaOracodeUserLevelId,
            Map<String, Object> additionalProperties) {
        this.name = name;
        this.prefix = prefix;
        this.checkInTime = checkInTime;
        this.checkOutTime = checkOutTime;
        this.is24Hour = is24Hour;
        this.isBiweeklyMode = isBiweeklyMode;
        this.isOneShot = isOneShot;
        this.isMaster = isMaster;
        this.extDormakabaOracodeUserLevelPrefix = extDormakabaOracodeUserLevelPrefix;
        this.dormakabaOracodeUserLevelId = dormakabaOracodeUserLevelId;
        this.additionalProperties = additionalProperties;
    }

    @JsonProperty("name")
    public String getName() {
        return name;
    }

    @JsonProperty("prefix")
    public double getPrefix() {
        return prefix;
    }

    @JsonProperty("check_in_time")
    public String getCheckInTime() {
        return checkInTime;
    }

    @JsonProperty("check_out_time")
    public String getCheckOutTime() {
        return checkOutTime;
    }

    @JsonProperty("is_24_hour")
    public boolean getIs24Hour() {
        return is24Hour;
    }

    @JsonProperty("is_biweekly_mode")
    public boolean getIsBiweeklyMode() {
        return isBiweeklyMode;
    }

    @JsonProperty("is_one_shot")
    public boolean getIsOneShot() {
        return isOneShot;
    }

    @JsonProperty("is_master")
    public boolean getIsMaster() {
        return isMaster;
    }

    @JsonProperty("ext_dormakaba_oracode_user_level_prefix")
    public double getExtDormakabaOracodeUserLevelPrefix() {
        return extDormakabaOracodeUserLevelPrefix;
    }

    @JsonProperty("dormakaba_oracode_user_level_id")
    public String getDormakabaOracodeUserLevelId() {
        return dormakabaOracodeUserLevelId;
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof DevicePropertiesDormakabaOracodeMetadataPredefinedTimeSlotsItem
                && equalTo((DevicePropertiesDormakabaOracodeMetadataPredefinedTimeSlotsItem) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(DevicePropertiesDormakabaOracodeMetadataPredefinedTimeSlotsItem other) {
        return name.equals(other.name)
                && prefix == other.prefix
                && checkInTime.equals(other.checkInTime)
                && checkOutTime.equals(other.checkOutTime)
                && is24Hour == other.is24Hour
                && isBiweeklyMode == other.isBiweeklyMode
                && isOneShot == other.isOneShot
                && isMaster == other.isMaster
                && extDormakabaOracodeUserLevelPrefix == other.extDormakabaOracodeUserLevelPrefix
                && dormakabaOracodeUserLevelId.equals(other.dormakabaOracodeUserLevelId);
    }

    @Override
    public int hashCode() {
        return Objects.hash(
                this.name,
                this.prefix,
                this.checkInTime,
                this.checkOutTime,
                this.is24Hour,
                this.isBiweeklyMode,
                this.isOneShot,
                this.isMaster,
                this.extDormakabaOracodeUserLevelPrefix,
                this.dormakabaOracodeUserLevelId);
    }

    @Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static NameStage builder() {
        return new Builder();
    }

    public interface NameStage {
        PrefixStage name(String name);

        Builder from(DevicePropertiesDormakabaOracodeMetadataPredefinedTimeSlotsItem other);
    }

    public interface PrefixStage {
        CheckInTimeStage prefix(double prefix);
    }

    public interface CheckInTimeStage {
        CheckOutTimeStage checkInTime(String checkInTime);
    }

    public interface CheckOutTimeStage {
        Is24HourStage checkOutTime(String checkOutTime);
    }

    public interface Is24HourStage {
        IsBiweeklyModeStage is24Hour(boolean is24Hour);
    }

    public interface IsBiweeklyModeStage {
        IsOneShotStage isBiweeklyMode(boolean isBiweeklyMode);
    }

    public interface IsOneShotStage {
        IsMasterStage isOneShot(boolean isOneShot);
    }

    public interface IsMasterStage {
        ExtDormakabaOracodeUserLevelPrefixStage isMaster(boolean isMaster);
    }

    public interface ExtDormakabaOracodeUserLevelPrefixStage {
        DormakabaOracodeUserLevelIdStage extDormakabaOracodeUserLevelPrefix(double extDormakabaOracodeUserLevelPrefix);
    }

    public interface DormakabaOracodeUserLevelIdStage {
        _FinalStage dormakabaOracodeUserLevelId(String dormakabaOracodeUserLevelId);
    }

    public interface _FinalStage {
        DevicePropertiesDormakabaOracodeMetadataPredefinedTimeSlotsItem build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements NameStage,
                    PrefixStage,
                    CheckInTimeStage,
                    CheckOutTimeStage,
                    Is24HourStage,
                    IsBiweeklyModeStage,
                    IsOneShotStage,
                    IsMasterStage,
                    ExtDormakabaOracodeUserLevelPrefixStage,
                    DormakabaOracodeUserLevelIdStage,
                    _FinalStage {
        private String name;

        private double prefix;

        private String checkInTime;

        private String checkOutTime;

        private boolean is24Hour;

        private boolean isBiweeklyMode;

        private boolean isOneShot;

        private boolean isMaster;

        private double extDormakabaOracodeUserLevelPrefix;

        private String dormakabaOracodeUserLevelId;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @Override
        public Builder from(DevicePropertiesDormakabaOracodeMetadataPredefinedTimeSlotsItem other) {
            name(other.getName());
            prefix(other.getPrefix());
            checkInTime(other.getCheckInTime());
            checkOutTime(other.getCheckOutTime());
            is24Hour(other.getIs24Hour());
            isBiweeklyMode(other.getIsBiweeklyMode());
            isOneShot(other.getIsOneShot());
            isMaster(other.getIsMaster());
            extDormakabaOracodeUserLevelPrefix(other.getExtDormakabaOracodeUserLevelPrefix());
            dormakabaOracodeUserLevelId(other.getDormakabaOracodeUserLevelId());
            return this;
        }

        @Override
        @JsonSetter("name")
        public PrefixStage name(String name) {
            this.name = name;
            return this;
        }

        @Override
        @JsonSetter("prefix")
        public CheckInTimeStage prefix(double prefix) {
            this.prefix = prefix;
            return this;
        }

        @Override
        @JsonSetter("check_in_time")
        public CheckOutTimeStage checkInTime(String checkInTime) {
            this.checkInTime = checkInTime;
            return this;
        }

        @Override
        @JsonSetter("check_out_time")
        public Is24HourStage checkOutTime(String checkOutTime) {
            this.checkOutTime = checkOutTime;
            return this;
        }

        @Override
        @JsonSetter("is_24_hour")
        public IsBiweeklyModeStage is24Hour(boolean is24Hour) {
            this.is24Hour = is24Hour;
            return this;
        }

        @Override
        @JsonSetter("is_biweekly_mode")
        public IsOneShotStage isBiweeklyMode(boolean isBiweeklyMode) {
            this.isBiweeklyMode = isBiweeklyMode;
            return this;
        }

        @Override
        @JsonSetter("is_one_shot")
        public IsMasterStage isOneShot(boolean isOneShot) {
            this.isOneShot = isOneShot;
            return this;
        }

        @Override
        @JsonSetter("is_master")
        public ExtDormakabaOracodeUserLevelPrefixStage isMaster(boolean isMaster) {
            this.isMaster = isMaster;
            return this;
        }

        @Override
        @JsonSetter("ext_dormakaba_oracode_user_level_prefix")
        public DormakabaOracodeUserLevelIdStage extDormakabaOracodeUserLevelPrefix(
                double extDormakabaOracodeUserLevelPrefix) {
            this.extDormakabaOracodeUserLevelPrefix = extDormakabaOracodeUserLevelPrefix;
            return this;
        }

        @Override
        @JsonSetter("dormakaba_oracode_user_level_id")
        public _FinalStage dormakabaOracodeUserLevelId(String dormakabaOracodeUserLevelId) {
            this.dormakabaOracodeUserLevelId = dormakabaOracodeUserLevelId;
            return this;
        }

        @Override
        public DevicePropertiesDormakabaOracodeMetadataPredefinedTimeSlotsItem build() {
            return new DevicePropertiesDormakabaOracodeMetadataPredefinedTimeSlotsItem(
                    name,
                    prefix,
                    checkInTime,
                    checkOutTime,
                    is24Hour,
                    isBiweeklyMode,
                    isOneShot,
                    isMaster,
                    extDormakabaOracodeUserLevelPrefix,
                    dormakabaOracodeUserLevelId,
                    additionalProperties);
        }
    }
}
